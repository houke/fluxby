meta {
  name: Haal adresboek entry op via IBAN
  type: http
  seq: 28
}

post {
  url: {{baseUrl}}/api/addressbook/by-iban
  body: json
  auth: none
}

headers {
  X-Profile-Id: {{profileId}}
}

body:json {
  {}
}

docs {
  # Haal adresboek entry op via IBAN
  
  Uses POST to avoid exposing sensitive IBAN in URL path/query (CodeQL js/sensitive-get-query).

  
  ## Endpoint
  `POST /api/addressbook/by-iban`
}
